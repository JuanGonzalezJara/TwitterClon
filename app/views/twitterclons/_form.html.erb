<%= form_with(model: twitterclon) do |form| %>
  <% if twitterclon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(twitterclon.errors.count, "error") %> prohibited this twitterclon from being saved:</h2>

      <ul>
        <% twitterclon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :username, style: "display: block" %>
    <%= form.text_field :username %>
  </div>
  
  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  

  <div>
    <%= form.submit %>
  </div>
<% end %>
